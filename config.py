# Single place to set the region/SDIS to analyse.
# All scripts (generate_dem.py, main.py, export.py, visualize.py) read this.
# It must match the folder name under data/, e.g. data/sdis-77/.
REGION = "sdis-77"
